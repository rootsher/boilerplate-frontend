import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <React.StrictMode>
    <h1>Hello World - <a href="/api/">/api</a></h1>
  </React.StrictMode>,
  document.getElementById('root')
);
